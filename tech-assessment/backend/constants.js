/**
 * Application constants.
 */
const UserRoles = {
  leader: 'Leader',
  manager: 'Manager',
  director: 'Director'
};

const UserStatus = {
  active: 'Active',
  inactive: 'Inactive'
};

const Passports = {
  jwt: 'jwt-bearer'
};


module.exports = {
  UserRoles,
  UserStatus,
  Passports
};
